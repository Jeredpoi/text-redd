# Простой Редактор

Текстовый редактор для Windows на Electron с богатым форматированием (как в Word) и проверкой орфографии на русском и английском языках.

## Возможности

- Боковая панель: поиск и список недавних файлов, оглавление текущего документа (переход по заголовкам H1–H3), сворачивается в узкую полосу
- Лента с двумя вкладками — «Главная» (форматирование) и «Вставка»
- Отдельное крупное поле заголовка документа над текстом (шрифт Lora), как в Word/заметках
- Всплывающая панель форматирования при выделении текста (жирный, курсив, подчёркнутый, зачёркнутый, выделение цветом)
- Несколько независимых окон одновременно (Файл → Новое окно, Ctrl+Shift+N) — можно редактировать разные документы параллельно, как в Блокноте
- Форматирование текста: жирный, курсив, подчёркнутый, зачёркнутый, цвет текста и выделения, шрифты и размеры
- Стили абзацев: заголовки H1–H3, обычный текст, цитата, код
- Списки (маркированные и нумерованные), отступы, выравнивание
- Вставка таблиц и ссылок через аккуратные диалоговые окна, вставка изображений и горизонтальных линий
- Проверка орфографии на лету (русский + английский) — красное подчёркивание ошибок и варианты исправления по правому клику
- Поиск и замена, со счётчиком совпадений
- Открытие/сохранение файлов — по умолчанию в .txt (как Блокнот), при необходимости можно сохранить в .html с полным форматированием (заголовок документа сохраняется как первый заголовок в файле)
- Экспорт в PDF и Word (.docx)
- Печать документа
- Тёмная и светлая тема (переключатель внизу боковой панели, применяется и к новым окнам)
- Счётчик слов и символов
- Имя файла и метка несохранённых изменений в заголовке окна и в статус-баре
- Предупреждение о несохранённых изменениях при закрытии

## Структура проекта

```
TextEditor/
├── main.js          — главный процесс Electron (меню, файлы, экспорт)
├── preload.js       — безопасный мост между main и renderer
├── index.html        — разметка редактора и панели инструментов
├── renderer.js       — логика редактора (форматирование, файлы, поиск)
├── styles.css        — оформление, светлая/тёмная тема
├── package.json       — зависимости и настройки сборки
└── build/
    ├── icon.ico       — иконка приложения
    └── build.bat      — скрипт для сборки установщика одной кнопкой
```

## Как собрать установщик (.exe)

Установщик нужно собирать на компьютере с Windows (кросс-сборка Windows-инсталлятора из Linux требует Wine, поэтому этот шаг нужно выполнить у себя).

1. Установите [Node.js](https://nodejs.org) (LTS-версия, 18 или новее).
2. Скопируйте папку `TextEditor` на свой компьютер.
3. Запустите `build\build.bat` двойным щелчком — он сам поставит зависимости и соберёт установщик.
4. Готовый установщик появится в папке `dist_installer\` (файл вида `Простой Редактор Setup 1.0.0.exe`).

Либо вручную из командной строки внутри папки проекта:

```
npm install
npm run dist
```

### Возможная ошибка при сборке: "symbolic link" / "privilege"

`electron-builder` при сборке скачивает вспомогательный архив с инструментами
и пытается создать в нём символические ссылки. По умолчанию Windows
запрещает это обычным пользователям, из-за чего сборка падает с ошибкой про
отсутствие прав. Есть два способа исправить:

1. Включить режим разработчика: **Параметры → Конфиденциальность и защита →
   Для разработчиков → Режим разработчика** (переключить в «Вкл»), затем
   запустить `build.bat` ещё раз. Это разовая настройка.
2. Либо запустить `build.bat` от имени администратора (правая кнопка мыши →
   «Запуск от имени администратора»).

## Запуск в режиме разработки (без сборки установщика)

```
npm install
npm start
```

## Настройка словаря

Проверка орфографии встроена в Electron/Chromium и включена для языков `ru` и `en-US` (см. `main.js`, вызов `setSpellCheckerLanguages`). Ничего дополнительно ставить не нужно — словари подтягиваются автоматически при первом запуске (нужен доступ в интернет один раз для скачивания hunspell-словарей).

## Шрифт страницы

Текст документа набран шрифтом Lora (Google Fonts) — подключается через интернет при запуске. Без подключения к интернету автоматически используется запасной шрифт (Georgia), интерфейс продолжает работать как обычно.

## Возможные доработки

- Автосохранение и восстановление после сбоя
- Вкладки внутри одного окна для нескольких документов (сейчас — несколько отдельных окон)
- Пользовательские шаблоны документов
- Синхронизация в облако
